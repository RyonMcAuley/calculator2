This calculator requires certain syntax to calculate.

Using input "20+20" will generate an error, but entering "20 + 20" will print the answer "40".
MUST INCLUDE SPACES BETWEEN NUMBERS AND OPERANDS

ONLY PERFORMS BASIC OPERATIONS
